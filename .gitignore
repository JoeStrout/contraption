.DS_Store

# Rendered ball sheets and frames.  Derived from art-sources/balls/make_balls.py;
# the sheets the game actually uses are committed under disk/pics.
art-sources/balls/out/

# 76 MB, too big to keep in history; the others in art-sources/models are
# small enough to commit.  Re-download it to re-render ball-tennis -- see
# "Imported models" in art-sources/balls/README.md.
art-sources/models/tennis_ball.glb

# Rendered prop strips.  Derived from art-sources/props/make_props.py; the
# strips the game actually uses are committed under disk/pics.
art-sources/props/out/

## Ignore the user data directory
disk/data
